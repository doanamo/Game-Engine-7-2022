/*
    Copyright (c) 2018 Piotr Doan. All rights reserved.
*/

#pragma once

namespace BuildInfo
{
    namespace Engine
    {
        const char* ChangeNumber = "${GIT_ENGINE_CHANGE_NUMBER}";
        const char* CommitHash = "${GIT_ENGINE_COMMIT_HASH}";
        const char* CommitDate = "${GIT_ENGINE_COMMIT_DATE}";
        const char* BranchName = "${GIT_ENGINE_BRANCH_NAME}";
    }

    namespace Project
    {
        const char* ChangeNumber = "${GIT_PROJECT_CHANGE_NUMBER}";
        const char* CommitHash = "${GIT_PROJECT_COMMIT_HASH}";
        const char* CommitDate = "${GIT_PROJECT_COMMIT_DATE}";
        const char* BranchName = "${GIT_PROJECT_BRANCH_NAME}";
    }
}
