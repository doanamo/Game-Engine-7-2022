/*
    Copyright (c) 2018 Piotr Doan. All rights reserved.
*/

#pragma once

namespace BuildInfo
{
    namespace Engine
    {
        const char* ChangeNumber = "${GIT_ENGINE_CHANGE_NUMBER}";
        const char* CommitHash = "${GIT_ENGINE_COMMIT_HASH}";
        const char* CommitDate = "${GIT_ENGINE_COMMIT_DATE}";
        const char* BranchName = "${GIT_ENGINE_BRANCH_NAME}";
    }

    namespace Game
    {
        const char* ChangeNumber = "${GIT_GAME_CHANGE_NUMBER}";
        const char* CommitHash = "${GIT_GAME_COMMIT_HASH}";
        const char* CommitDate = "${GIT_GAME_COMMIT_DATE}";
        const char* BranchName = "${GIT_GAME_BRANCH_NAME}";
    }
}
